name: Daily Build

on:
  schedule:
    # 每天北京时间 08:00 = UTC 00:00
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle17-${{ hashFiles('**/*.gradle*') }}
          restore-keys: gradle17-

      - name: Clear 18PlusList.txt
        run: echo "">$GITHUB_WORKSPACE/app/src/main/assets/18PlusList.txt

      - name: Clear Gradle Cache
        run: |
          rm -rf ~/.gradle/caches/transforms-* ~/.gradle/caches/jars-* ~/.gradle/caches/build-cache-* ~/.gradle/caches/journal-* ~/.gradle/caches/modules-*/files-* 2>/dev/null || true
          rm -rf ~/.gradle/daemon 2>/dev/null || true
          rm -rf $GITHUB_WORKSPACE/.gradle 2>/dev/null || true
          rm -rf $GITHUB_WORKSPACE/app/build 2>/dev/null || true

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build APK
        run: |
          chmod +x gradlew
          ./gradlew --stop 2>/dev/null || true
          ./gradlew assembleAppRelease --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: legado-md3-apk
          path: app/build/outputs/apk/app/release/*arm64-v8a*.apk
          retention-days: 2

  notify:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download APK
        uses: actions/download-artifact@v4
        with:
          name: legado-md3-apk
          path: ./apk

      - name: Get APK info
        id: apk
        run: |
          APK=$(find ./apk -name "*.apk" | head -1)
          echo "path=$APK" >> $GITHUB_OUTPUT
          echo "size=$(du -h $APK | cut -f1)" >> $GITHUB_OUTPUT
          echo "name=$(basename $APK)" >> $GITHUB_OUTPUT
          echo "date=$(date '+%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: daily-${{ steps.apk.outputs.date }}
          name: Daily Build ${{ steps.apk.outputs.date }}
          body: |
            Legado MD3 Daily Build

            📦 APK: ${{ steps.apk.outputs.name }}
            📏 Size: ${{ steps.apk.outputs.size }}
            🏗️ Branch: main

            💬 Issue: https://github.com/fengs2021/legado-with-MD3/releases
          files: ${{ steps.apk.outputs.path }}
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send Feishu Notification
        if: env.FEISHU_WEBHOOK != ''
        run: |
          curl -s -X POST "${{ vars.FEISHU_WEBHOOK }}" \
            -H "Content-Type: application/json" \
            -d '{
              "msg_type": "text",
              "content": {
                "text": "✅ Legado MD3 Daily Build 已完成\n📅 日期: '"${{ steps.apk.outputs.date }}"' 📦 大小: ${{ steps.apk.outputs.size }}\n⬇️ 下载: https://github.com/${{ github.repository }}/releases/tag/daily-${{ steps.apk.outputs.date }}"
              }
            }'
